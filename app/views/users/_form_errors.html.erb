<% if @user.errors.any? %>
  <h2>The following errors prevented the user to be sign up</h2>
  <ul>
    <% @user.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>